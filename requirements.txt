# Core dependencies
anthropic>=0.21.0
supabase>=2.4.0
python-dotenv>=1.0.0
httpx>=0.27.0

# MCP (Model Context Protocol) support
# Note: MCP module may need to be installed separately
# mcp>=0.1.0

# A2A (Application-to-Agent) Protocol support
# Note: A2A SDK is optional - worker will function without it
# a2a-sdk>=0.1.0

# Async support
asyncio>=3.4.3

# Utilities
python-dateutil>=2.8.2

# Development dependencies (optional)
pytest>=7.4.0
pytest-asyncio>=0.21.0
black>=23.0.0
flake8>=6.0.0